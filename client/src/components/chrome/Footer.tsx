export function Footer() {
  return (
    <footer className="border-t border-border px-4 py-6 text-xs leading-relaxed text-muted sm:px-8">
      Prices labeled "est all-in" include the listing's displayed total for the dates above plus a
      14% LA transient-occupancy-tax buffer. Airbnb cards generally show a total that already
      includes the cleaning fee but excludes tax — verify with each listing's price breakdown at
      the booking step. "Check manually" entries didn't return a date-specific quote during the
      run. Distances are approximate driving miles to Downtown LA (City Hall).
    </footer>
  );
}

{
  "name": "grouppad",
  "version": "0.2.0",
  "description": "GroupPad — LA group trip rental aggregator with daily pipeline",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "pipeline": "node pipeline.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0",
    "express": "^4.21.2",
    "playwright-core": "^1.60.0"
  }
}
